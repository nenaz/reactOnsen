import React from 'react'

const Loading = (props) => {
    return (
        <div style={{
            width: '100%',
            height: '100vh',
            backgroundColor: 'red'
        }}>
        </div>
    )
}   

export default Loading